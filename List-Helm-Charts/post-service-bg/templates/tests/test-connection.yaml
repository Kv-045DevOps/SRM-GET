apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "post-service-bg.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "post-service-bg.name" . }}
    helm.sh/chart: {{ include "post-service-bg.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "post-service-bg.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
