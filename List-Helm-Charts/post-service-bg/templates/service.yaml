apiVersion: v1
kind: Service
metadata:
  name: {{ include "post-service-bg.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "post-service-bg.name" . }}
    helm.sh/chart: {{ include "post-service-bg.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "post-service-bg.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
