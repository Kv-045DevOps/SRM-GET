apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "e2e-testing-post.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "e2e-testing-post.name" . }}
    helm.sh/chart: {{ include "e2e-testing-post.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "e2e-testing-post.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
