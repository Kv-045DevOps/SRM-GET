apiVersion: v1
kind: Service
metadata:
  name: {{ include "e2e-testing-post.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "e2e-testing-post.name" . }}
    helm.sh/chart: {{ include "e2e-testing-post.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "e2e-testing-post.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
